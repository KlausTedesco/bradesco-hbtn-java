/*
 * import java.util.ArrayList;
 * 
 * public class ProcessadorVideo { ArrayList<CanalNotificacao> canais = new
 * ArrayList<CanalNotificacao>();
 * 
 * void registrarCanal(CanalNotificacao canal){ //Add canal a lista de canais
 * canais.add(canal); }
 * 
 * void processar(Video video) { //Esse método deve percorrer por todos os
 * canais registros //e notificar a mensagem <arquivo> - <formato> do tipo LOG
 * for(CanalNotificacao canal : canais) { String mensagem =
 * video.arquivo+" - "+video.formatoVideo;
 * 
 * canal.notificar(new Mensagem(mensagem, TipoMensagem.LOG)); } } }
 */